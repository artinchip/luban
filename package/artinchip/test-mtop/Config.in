menuconfig BR2_PACKAGE_TEST_MTOP
	bool "test-mtop"
	default y

if BR2_PACKAGE_TEST_MTOP
config BR2_PACKAGE_TEST_MTOP_USE_PREBUILT
	bool "use prebuilt binary instead of building from source"
	default n
endif
