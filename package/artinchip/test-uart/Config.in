menuconfig BR2_PACKAGE_TEST_UART
	bool "test-uart"
	default y

if BR2_PACKAGE_TEST_UART
config BR2_PACKAGE_TEST_UART_USE_PREBUILT
	bool "use prebuilt binary instead of building from source"
	default n
endif
