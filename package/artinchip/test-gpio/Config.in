menuconfig BR2_PACKAGE_TEST_GPIO
	bool "test-gpio"
	default y

if BR2_PACKAGE_TEST_GPIO
config BR2_PACKAGE_TEST_GPIO_USE_PREBUILT
	bool "use prebuilt binary instead of building from source"
	default n
endif
