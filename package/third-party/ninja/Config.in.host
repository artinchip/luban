menuconfig BR2_PACKAGE_HOST_NINJA
	bool "host ninja"

if BR2_PACKAGE_HOST_NINJA
config BR2_PACKAGE_HOST_NINJA_USE_PREBUILT
	bool "use prebuilt binary instead of building from source"
	default y
endif
