menuconfig BR2_PACKAGE_HOST_ZLIG_NG
	bool "host zlib-ng"
	depends on BR2_PACKAGE_ZLIG_NG

if BR2_PACKAGE_HOST_ZLIG_NG
config BR2_PACKAGE_HOST_ZLIG_NG_USE_PREBUILT
	bool "use prebuilt binary instead of building from source"
	default y
endif

