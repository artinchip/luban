menuconfig BR2_PACKAGE_HOST_BZIP2
	bool "host bzip2"
	default n

if BR2_PACKAGE_HOST_BZIP2
config BR2_PACKAGE_HOST_BZIP2_USE_PREBUILT
	bool "use prebuilt binary instead of building from source"
	default y
endif
