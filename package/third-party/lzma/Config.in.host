menuconfig BR2_PACKAGE_HOST_LZMA
	bool "host lzma"
	default n

if BR2_PACKAGE_HOST_LZMA
config BR2_PACKAGE_HOST_LZMA_USE_PREBUILT
	bool "use prebuilt binary instead of building from source"
	default y
endif
