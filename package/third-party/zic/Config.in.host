menuconfig BR2_PACKAGE_HOST_ZIC
	bool "host zic"
	default y

if BR2_PACKAGE_HOST_ZIC
config BR2_PACKAGE_HOST_ZIC_USE_PREBUILT
	bool "use prebuilt binary instead of building from source"
	default y
endif
