menuconfig BR2_PACKAGE_HOST_BISON
	bool "host bison"
	default y

if BR2_PACKAGE_HOST_BISON
config BR2_PACKAGE_HOST_BISON_USE_PREBUILT
	bool "use prebuilt binary instead of building from source"
	default y
endif
