menuconfig BR2_PACKAGE_HOST_LIBZLIB
	bool "host libzlib"
	depends on BR2_PACKAGE_LIBZLIB

if BR2_PACKAGE_HOST_LIBZLIB
config BR2_PACKAGE_HOST_LIBZLIB_USE_PREBUILT
	bool "use prebuilt binary instead of building from source"
	default y
endif

