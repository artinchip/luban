menuconfig BR2_PACKAGE_HOST_MAKEDEVS
	bool "host makedevs"
	default y

if BR2_PACKAGE_HOST_MAKEDEVS
config BR2_PACKAGE_HOST_MAKEDEVS_USE_PREBUILT
	bool "use prebuilt binary instead of building from source"
	default y
endif
