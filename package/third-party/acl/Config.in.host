menuconfig BR2_PACKAGE_HOST_ACL
	bool "host acl"
	select BR2_PACKAGE_HOST_ATTR

if BR2_PACKAGE_HOST_ACL
config BR2_PACKAGE_HOST_ACL_USE_PREBUILT
	bool "use prebuilt binary instead of building from source"
	default y
endif
