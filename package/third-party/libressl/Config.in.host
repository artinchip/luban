menuconfig BR2_PACKAGE_HOST_LIBRESSL
	bool "host libressl"
	depends on BR2_PACKAGE_LIBRESSL

if BR2_PACKAGE_HOST_LIBRESSL
config BR2_PACKAGE_HOST_LIBRESSL_USE_PREBUILT
	bool "use prebuilt binary instead of building from source"
	default y
endif

