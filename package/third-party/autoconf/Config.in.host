menuconfig BR2_PACKAGE_HOST_AUTOCONF
	bool "host autoconf"
	default y

if BR2_PACKAGE_HOST_AUTOCONF
config BR2_PACKAGE_HOST_AUTOCONF_USE_PREBUILT
	bool "use prebuilt binary instead of building from source"
	default y
endif
