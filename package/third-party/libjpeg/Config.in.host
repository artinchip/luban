menuconfig BR2_PACKAGE_HOST_LIBJPEG
	bool "host libjpeg"
	depends on BR2_PACKAGE_LIBJPEG

if BR2_PACKAGE_HOST_LIBJPEG
config BR2_PACKAGE_HOST_LIBJPEG_USE_PREBUILT
	bool "use prebuilt binary instead of building from source"
	default y
endif

