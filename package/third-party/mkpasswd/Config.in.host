menuconfig BR2_PACKAGE_HOST_MKPASSWD
	bool "host mkpasswd"
	default y

if BR2_PACKAGE_HOST_MKPASSWD
config BR2_PACKAGE_HOST_MKPASSWD_USE_PREBUILT
	bool "use prebuilt binary instead of building from source"
	default y
endif
