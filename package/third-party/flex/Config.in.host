menuconfig BR2_PACKAGE_HOST_FLEX
	bool "host flex"
	default n

if BR2_PACKAGE_HOST_FLEX
config BR2_PACKAGE_HOST_FLEX_USE_PREBUILT
	bool "use prebuilt binary instead of building from source"
	default y
endif
